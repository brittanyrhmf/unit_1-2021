# create database and save players info

score_file = "score.txt"
s = open(score_file, "a")
line = name_1 + '=' + str(score1) + '\n' + name_2 + '=' + str(score2) + '\n'
s.write(line)
s.close()
s = open(score_file, "r")
print()
print('final scores')
print(s.read())
s.close()
